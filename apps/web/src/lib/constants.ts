/** Number of days a new workspace gets on the free trial. */
export const TRIAL_DAYS = 14;

/** How long an invitation token is valid before it expires. */
export const INVITE_EXPIRY_DAYS = 7;

/** Milliseconds in a day — reused for trial / expiry math. */
export const ONE_DAY_MS = 24 * 60 * 60 * 1000;
